// eslint-disable-next-line camelcase
import { Exo_2 } from 'next/font/google';

export const exo2 = Exo_2({
  weight: 'variable',
  subsets: ['latin'],
});

export const exo2Italic = Exo_2({
  weight: 'variable',
  style: 'italic',
  subsets: ['latin'],
});
